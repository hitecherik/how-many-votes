{
  "name": "how-many-votes",
  "version": "1.0.0",
  "description": "Finds the number of votes a person has nationally based on their constituency",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Nielsen",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^5.2.1",
    "express": "^4.16.4",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5"
  }
}
